# some hacks for when we use backports to generate a package
# to build modules out of tree.
config WIRELESS
	def_bool y
config NET_CORE
	def_bool y
config EXPERT
	def_bool y
config MAC80211
	def_tristate m
config WLCORE_SDIO
	def_tristate m